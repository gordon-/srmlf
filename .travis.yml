# We do not use sudo
sudo: false

# Python versions for matrix
language: python
python:
  - "3.5"

env:
 - TOX_ENV=py33
 - TOX_ENV=py34
 - TOX_ENV=py35
 - TOX_ENV=flake8
install:
 - pip install tox coveralls
script:
 - tox -e $TOX_ENV
 - coverage run setup.py test

after_sucess:
 - coveralls
